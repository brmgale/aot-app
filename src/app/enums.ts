export enum CdOrInvest {
    cd = 'Cd',
    invest = 'Invest'
}

export enum ImmediateOrAtMaturity {
    immediate = 'Immediate',
    atMaturity = 'At Maturity',
    none = 'none'
}

export enum FullPartial {
    full = 'Full',
    partial = 'Partial',
    none = 'none'
}